module.exports = {
    testEnvironment: 'node',
    testMatch: ['**/tests/**/*.test.js'],
    forceExit: true,
    verbose: true,
    clearMocks: true,
    setupFilesAfterEnv: [], // Can add setup files here later if needed
};
